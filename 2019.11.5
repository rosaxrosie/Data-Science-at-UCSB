git config --global user.name "Rosa Pan"
git.config --global user.email "rpan@ucsb.edu"
git config user.name
cd desktop/DataScienceClub
git clone git clone https://github.com/rosaxrosie/DS-Progress-Journal
git add -A
git commit -m "Created my first activity log"
git push
